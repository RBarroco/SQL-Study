# Local variables are variables within BEGIN/END
# Session variables are variables within a session; each connection to a server holds its own "session" independent the others connections;
# Global variables are varaibles that every single session can have access to.
	# system variables
		# .max_connections()
		# .max_join_size()

