Sentences in SQL;

AND
OR
IN / NOT IN
LIKE / NOT LIKE
BETWEEN... AND...
EXISTS / NOT EXISTS
IS NULL / IS NOT NULL
Comparison operators
etc.

mySQL:

AND > OR

% - matches a sequence of characters
_ - matches a single character